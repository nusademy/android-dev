<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.nusademy.nusademy"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- <uses-permission android:name="android.permission.WAKE_LOCK" /> -->
    <!-- <uses-feature android:name="android.hardware.type.watch" /> -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nusademy">
        <activity android:name=".ui.editteacher.EditTeacherActivity"></activity>
        <activity android:name=".ui.signup.SignUpActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.chatbot.ChatbotActivity" /> <!-- <uses-library -->
        <!-- android:name="com.google.android.wearable" -->
        <!-- android:required="true" /> -->
        <!-- &lt;!&ndash; -->
        <!-- Set to true if your app is Standalone, that is, it does not require the handheld -->
        <!-- app to run. -->
        <!-- &ndash;&gt; -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.wearable.standalone" -->
        <!-- android:value="true" /> -->
        <activity
            android:name=".ui.school_profil2.SchoolProfil2Activity"
            android:label="@string/title_activity_school_profil2" />
        <activity
            android:name=".ui.teacher_profil.TeacherProfilActivity"
            android:label="@string/title_activity_teacher_profil" />
        <activity
            android:name=".ui.school_profil.SchoolProfilActivity"
            android:label="@string/title_activity_school_profil" />
        <activity
            android:name=".ui.login_option.LoginOptionActivity"
            android:label="@string/title_activity_login_option" />
        <activity
            android:name=".ui.firstscreen.FirstScreenActivity"
            android:label="@string/title_activity_first_screen" />
        <activity
            android:name=".ui.about.AboutActivity"
            android:label="@string/title_activity_about"
            android:theme="@style/Theme.Nusademy.NoActionBar" />
        <activity
            android:name=".ui.profile.UserProfileActivity"
            android:label="@string/title_activity_user_profile"
            android:theme="@style/Theme.Nusademy.NoActionBar" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.UserHomeActivity" />
    </application>

</manifest>